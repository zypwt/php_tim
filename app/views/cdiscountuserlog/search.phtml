<?php $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo \Phalcon\Tag::linkTo(array("cdiscountuserlog/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo \Phalcon\Tag::linkTo(array("cdiscountuserlog/new", "Create c discount user log")); ?>
        </td>
    <tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>ID</th>
            <th>USER Of ID</th>
            <th>COMM</th>
            <th>CREATE Of TIME</th>
        </tr>
    </thead>
    <tbody>
    <?php
        if(isset($page->items)){
            foreach($page->items as $cdiscountuserlog){ ?>
        <tr>
            <td><?php echo $cdiscountuserlog->ID ?></td>
            <td><?php echo $cdiscountuserlog->USER_ID ?></td>
            <td><?php echo $cdiscountuserlog->COMM ?></td>
            <td><?php echo $cdiscountuserlog->CREATE_TIME ?></td>
            <td><?php echo \Phalcon\Tag::linkTo(array("cdiscountuserlog/edit/".$cdiscountuserlog->ID, "Edit")); ?></td>
            <td><?php echo \Phalcon\Tag::linkTo(array("cdiscountuserlog/delete/".$cdiscountuserlog->ID, "Delete")); ?></td>
        </tr>
    <?php }
        } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="4" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo \Phalcon\Tag::linkTo("cdiscountuserlog/search", "First") ?></td>
                        <td><?php echo \Phalcon\Tag::linkTo("cdiscountuserlog/search?page=".$page->before, "Previous") ?></td>
                        <td><?php echo \Phalcon\Tag::linkTo("cdiscountuserlog/search?page=".$page->next, "Next") ?></td>
                        <td><?php echo \Phalcon\Tag::linkTo("cdiscountuserlog/search?page=".$page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    <tbody>
</table>